<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="co.yedam.MidProject.student.service.StudentMapper">
	<select id="selectStudentList" resultType="co.yedam.MidProject.student.service.StudentVO">
		SELECT * FROM STUDENT
	</select>
	
	<!-- 로그인 -->
	<!-- <select id="selectMember" parameterType="co.yedam.MidProject.member.service.StudentVO"
		resultType="co.yedam.MidProject.member.service.StudentVO">
		 SELECT * FROM MEMBER WHERE S_ID = #{id}
		 <if test = "password != null">AND S_PASSWORD = #{password}</if>
	</select>
	 -->
	
	<insert id="insertStudent" parameterType="co.yedam.MidProject.student.service.StudentVO"></insert>
	
	<update id="updateStudent" parameterType="co.yedam.MidProject.student.service.StudentVO"></update>
	
	<delete id="deleteStudent" parameterType="co.yedam.MidProject.student.service.StudentVO"></delete>
	<!-- 중복체크 -->
	<!-- <select id="isIdCheck" parameterType="String"></select> -->
</mapper>